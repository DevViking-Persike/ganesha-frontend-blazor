Vou apresentar a decisão de escopo para você.

Antes de redigir o documento de especificação, preciso que você escolha **qual incremento** ele deve especificar. O Diagnóstico Arquitetural de 2026-07-05 (a "descoberta" de fato, já que não há `.spec/STATE.md` nem instância formal de discovery) prioriza estes candidatos, todos de baixo risco:

| Opção | Incremento | Modo | O que resolve |
|---|---|---|---|
| **FE-1** | Quebrar `LabCharts.razor` (941→≤300 linhas) | REFATORAR | Violação mais grave da Regra 1 (tamanho); split por família de chart + extrair mock data para `ChartsSamples.cs` |
| **FE-2** | Quebrar `LabGrid.razor` (510→≤300 linhas) | REFATORAR | Segunda violação da Regra 1; split em seções/sub-páginas |
| **FE-DOC-1** | Corrigir drift dos 3 docs de análise (`01`, `03`, `05`) | DOCUMENTAR | O grafo de conhecimento está indexando afirmações falsas ("sem testes", "7 charts"); o diagnóstico recomenda fazer isto **antes** de qualquer próxima etapa |
| **FE-1 + FE-2** | Ambas as páginas-lab oversize num só incremento | REFATORAR | Resolve P1 inteiro (todas as violações de tamanho de página-lab) de uma vez |

**Minha recomendação:** **FE-1**. É a violação mais grave (941 linhas, quase 2× o teto), tem fronteira técnica clara (8 seções de chart + `@code` de mock data separável), e a suíte bUnit já existe para servir de teste de caracterização (não-regressão). É o melhor exemplar de uma spec de refatoração que o design gate consegue aprovar com confiança.

Observação: o diagnóstico sinaliza FE-DOC-1 como "antes de qualquer próxima etapa" — mas isso é governança de pipeline, não impede uma spec de refatoração de ser escrita (posso registrar a dependência de drift como premissa/risco dentro da própria spec de FE-1).

Qual incremento você quer que eu especifique?