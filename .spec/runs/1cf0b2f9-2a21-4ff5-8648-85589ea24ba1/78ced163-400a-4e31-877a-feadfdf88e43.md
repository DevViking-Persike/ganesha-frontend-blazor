Plano escrito em `/Users/persike/.claude/plans/execute-a-etapa-planner-dynamic-dragonfly.md`. (`ExitPlanMode`/`AskUserQuestion` não estão disponíveis neste toolset; conforme o prompt, o entregável da etapa Planner é o próprio plano como resposta final — abaixo.)

---

# Plano de Tarefas — Etapa **Planner** (papel `Plan`)

**Pipeline:** njord · **Run:** `1cf0b2f9-2a21-4ff5-8648-85589ea24ba1` · **Incremento:** **FE-1** (recomendado pela Spec Generation `4fab4f8c`) · **Modo:** REFATORAR

## Contexto (por que)
`LabCharts.razor` tem **941 linhas** (quase 2× o teto da Regra 1) — a pior violação de tamanho do repo (P1 do Diagnóstico `047d0537`). Fronteira técnica clara: 7 seções de chart + `@code` com 222 linhas de mock data puro (zero lógica). Suíte bUnit existe como rede de caracterização. FE-1 é **split puro** (sem migrar estilos inline = FE-5, sem introduzir camada de domínio = Regra 5).

## Objetivo e DoD
Reduzir `LabCharts.razor` de 941 → **≤300 linhas**, markup renderizado idêntico.
- Todos os arquivos (novos e modificados) ≤ 300 linhas.
- `dotnet build` verde; `dotnet test` verde (zero regressão — `LabChartsPageTests` mantém o contrato das 7 seções).
- Renderização de `/lab/charts` visualmente idêntica (validação manual delegada — Regra 8).
- Direção de dependência preservada (sub-componentes em `Lab/Pages`, consumidores do DS `Gns*`).
- Commits isolados por motivo (Regra 6).

## Estratégia — 2 commits obrigatórios (+ 1 opcional)

**Commit 0 (opcional, recomendado) — `test:`** Endurecer `LabChartsPageTests` com asserções de contagem (nº de seções; labels por família) antes de mexer — rede de segurança Regra 6.

**Commit 1 — `refactor: extrai dados mockados para LabChartsSamples.cs`**
- Criar `Components/Lab/Pages/LabChartsSamples.cs`: `internal static class` no namespace `Lab.Pages` (mesmo namespace da página → sem `@using`), com as **28 coleções** de L724–940 (`MonthlyRevenue` … `MarketAnalysis`) como `internal static readonly IReadOnlyList<T>`.
- Como é `.cs`, `_Imports.razor` **não** vale — declarar `using` explícitos para `DesignSystem.Charts` (`ChartDataPoint`/`ChartSeries`/`ProcessStep`/`BubbleDataPoint`) e `Composites.ProjectTimeline`. Precedente: `ProjectTimelineModels.cs`.
- Em `LabCharts.razor`: remover o `@code` inteiro; trocar `_monthlyRevenue` → `LabChartsSamples.MonthlyRevenue` nos `Data=`/`Labels=`.
- Página 941 → **~720** (intermediário; Commit 2 fecha).

**Commit 2 — `refactor: extrai seções de showcase em sub-componentes`**
Extrair as **3 maiores seções** (suficiente para ≤300; Regra 5 — não antecipar simetria):

| Novo arquivo | Seção origem | LOC |
|---|---|---|
| `LabChartsLineChartsSection.razor` | Seção 2 (L61–174) | ~120 |
| `LabChartsInfographicSection.razor` | Seção 5 (L326–518) | ~200 |
| `LabChartsDashboardSection.razor` | Seção 7 (L549–716) | ~175 |

Cada um: `@namespace …Lab.Pages`, lê `LabChartsSamples.*` direto (**sem** `[Parameter]` — over-engineering para showcase), renderiza o mesmo `<GnsSection>…<GnsCard>…`.
Página ~720 → **~250 linhas** ✅.

**Contagem final (todos ≤300):** `LabCharts.razor` ~250 · `LabChartsSamples.cs` ~230 · `Infographic` ~200 · `Dashboard` ~175 · `LineCharts` ~120.

## Arquivos
- **Modificar:** `LabCharts.razor`; `LabChartsPageTests.cs` (Commit 0).
- **Criar:** `LabChartsSamples.cs`, `LabChartsLineChartsSection.razor`, `LabChartsInfographicSection.razor`, `LabChartsDashboardSection.razor` (todos em `Components/Lab/Pages/`).
- **Reutilizar:** `ProjectTimelineModels.cs` (precedente `*Models.cs`), `ChartModels.cs` (tipos dos dados), `_Imports.razor` (namespaces `Gns*` globais).

## Fora de escopo (incrementos próprios)
FE-5 (155 estilos inline → CSS scoped), FE-2 (`LabGrid.razor` 510 — será o 2º consumidor deste template), FE-3/FE-4 (coverlet + Stryker, Regra 2), FE-DOC-1 (drift dos 3 docs — premissa, não bloqueia).

## Riscos
- `.cs` não enxerga `_Imports.razor` → `using` explícitos (Commit 1).
- Sub-componente de showcase é **primeiro do gênero** no repo → introdução deliberada justificada pela Regra 1; registrar no gate de Arquitetura como novo padrão (FE-2 seguirá o mesmo molde).
- Teste smoke fraco → Commit 0 opcional.

## Verificação
1. **Executo (dev):** `dotnet build Ganesha.DesignLab.slnx` + `dotnet test` verde a cada commit; `wc -l` confirma ≤300 em todos os 5 arquivos; `git diff --stat` confinado a `Components/Lab/Pages/`; `git grep -n '_monthlyRevenue\b'` vazio (sem underscore-id órfão).
2. **Delego ao usuário (Regra 8):** `./scripts/full-stack/start-all.sh` → `/lab/charts` → confirmar as 7 seções (Bar, Line/Sparklines, Donut, Progress, Infographic, Timeline, Dashboard) idênticas ao pré-split.

## Próxima etapa
Aprovado no gate de **Arquitetura** (review), destrava **Desenvolvimento** (Dev), que executa os commits na ordem acima.